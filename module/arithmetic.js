// Addition
const d = 3 + 2.5;

// Subtraction
const e = 3 - 1.7;

// Multiplication
const f = 5 * 4;

// Division
const g = 5 / 2;

// Floor
const g1 = Math.floor(5 / 2);

// Modulus operator
const h = 8 % 5;
// h = 3

// a = (Math.floor(a / b) * b) + (a % b)
